{
  "name": "ppk-duty-node-backend",
  "version": "4.0.0",
  "description": "PPK Duty Node.js backend with Socket.IO realtime support; admin is system-only and never stored in users",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "socket.io": "^4.7.5"
  }
}
